# T1.1 + T1.2 学习指南与改进计划

> 完成时间：2026-06-20  
> 任务：StateGraph基础架构 + 条件分支ReAct循环  
> 状态：✅ 已完成（含已知问题和改进计划）

---

## 📋 目录

1. [完成内容总览](#完成内容总览)
2. [已知问题说明](#已知问题说明)
3. [改进计划](#改进计划)
4. [核心学习内容](#核心学习内容)
5. [技术深度解析](#技术深度解析)
6. [面试准备材料](#面试准备材料)
7. [下一步行动](#下一步行动)

---

## 完成内容总览

### T1.1 StateGraph基础架构 ✅

**交付物：**
- ✅ 状态定义（state.py）
- ✅ 检索节点（retrieve_node.py）
- ✅ 回答生成节点（answer_node.py）
- ✅ 基础图（basic_graph.py）
- ✅ 测试套件（6/6通过）
- ✅ 使用示例

**流程：**
```
START → retrieve → answer → END
```

**关键特性：**
- TypedDict状态管理
- Annotated + operator.add消息追加
- pathlib.Path绝对路径
- logging日志系统
- 完善的错误处理

### T1.2 条件分支+ReAct循环 ✅

**交付物：**
- ✅ 条件判断函数（conditions.py）
- ✅ 查询改写节点（rewrite_node.py）
- ✅ Agent推理节点（agent_node.py）⚠️ Mock模式
- ✅ 工具执行节点（tools_node.py）
- ✅ ReAct图（react_graph.py）
- ✅ 测试套件（7/7通过）

**流程：**
```
START → rewrite → retrieve → agent → should_continue
                                ↓              ↓
                            answer ←────── tools
                                ↓              ↓
                               END        (loop back)
```

**关键特性：**
- add_conditional_edges条件路由
- should_continue循环判断
- max_iterations防无限循环
- ReAct循环（Think-Act-Observe-Repeat）

---

## 已知问题说明

### ⚠️ 问题1：Agent节点使用Mock LLM响应

**位置：** `src/modules/module_5_langgraph/nodes/agent_node.py`

**问题描述：**
```python
# 当前实现（简化Mock版本）
mock_response = AIMessage(content=f"我已分析您的问题：{state['query']}")
tool_calls = []  # 不生成真实的tool_calls
```

**原因：**
在测试过程中，通义千问API调用出现以下错误：
```
KeyError: 'request'
File "langchain_community/chat_models/tongyi.py", line 674, in _generate
    resp = self.completion_with_retry(**params)
```

**影响范围：**
- Agent节点无法调用真实LLM进行推理
- 无法生成真实的tool_calls
- ReAct循环无法触发真实的工具调用
- 只能测试图结构和条件路由逻辑

**当前工作状态：**
- ✅ 图结构正确
- ✅ 条件路由正常工作
- ✅ 循环控制有效
- ⚠️ Agent推理使用Mock数据
- ⚠️ 工具调用未实际触发

---

## 改进计划

### 🔧 Phase 1：修复LLM API调用（优先级：P0）

**任务清单：**

#### 1.1 诊断API问题
- [ ] 检查DASHSCOPE_API_KEY配置
- [ ] 测试通义千问API连接
- [ ] 检查langchain_community版本兼容性
- [ ] 查看dashscope SDK版本

**诊断命令：**
```bash
# 检查环境变量
echo $DASHSCOPE_API_KEY

# 测试LLM调用
python -c "from src.models.llm import get_llm; llm = get_llm(); print(llm.invoke('你好'))"

# 检查版本
pip list | grep -E "langchain|dashscope"
```

#### 1.2 修复agent_node.py
- [ ] 恢复真实LLM调用
- [ ] 实现工具绑定（llm.bind_tools()）
- [ ] 处理tool_calls生成
- [ ] 添加重试逻辑

**修复后的代码结构：**
```python
def agent_node(state: TravelAgentState) -> Dict[str, Any]:
    """Agent推理节点：真实LLM版本"""
    
    # 1. 获取LLM
    llm = get_llm(temperature=0.7)
    
    # 2. 绑定工具
    from src.modules.module_3_react_agent.tools import get_all_tools
    tools = get_all_tools()
    llm_with_tools = llm.bind_tools(tools)
    
    # 3. 构建消息
    messages = build_messages(state)
    
    # 4. 调用LLM（带重试）
    try:
        response = llm_with_tools.invoke(messages)
        tool_calls = extract_tool_calls(response)
        
        return {
            "messages": [response],
            "tool_calls": tool_calls,
            "iteration": state["iteration"] + 1
        }
    except Exception as e:
        logger.error(f"LLM调用失败：{e}")
        # 降级处理
        return fallback_response(state)
```

#### 1.3 实现真实工具调用
- [ ] 集成现有工具（weather, hotel, flight）
- [ ] 实现tools_node的真实执行逻辑
- [ ] 添加工具错误处理
- [ ] 记录工具调用日志

**tools_node改进：**
```python
def tools_node(state: TravelAgentState) -> Dict[str, Any]:
    """工具执行节点：真实版本"""
    
    tool_calls = state.get("tool_calls", [])
    if not tool_calls:
        return {"messages": [], "tool_calls": []}
    
    # 创建工具映射
    tool_map = create_tool_map()
    
    tool_messages = []
    for tool_call in tool_calls:
        tool_name = tool_call['name']
        tool_args = tool_call['args']
        
        try:
            # 真实执行工具
            result = tool_map[tool_name].invoke(tool_args)
            
            tool_messages.append(ToolMessage(
                content=str(result),
                tool_call_id=tool_call['id'],
                name=tool_name
            ))
            
            logger.info(f"✅ 工具执行成功：{tool_name}")
            
        except Exception as e:
            logger.error(f"❌ 工具执行失败：{tool_name} - {e}")
            
            tool_messages.append(ToolMessage(
                content=f"工具执行失败：{str(e)}",
                tool_call_id=tool_call['id'],
                name=tool_name
            ))
    
    return {
        "messages": tool_messages,
        "tool_calls": []
    }
```

#### 1.4 端到端测试
- [ ] 测试真实LLM调用
- [ ] 测试工具调用生成
- [ ] 测试ReAct循环触发
- [ ] 测试多轮对话场景

**测试用例：**
```python
def test_real_llm_agent():
    """测试真实LLM Agent"""
    query = "查询北京今天的天气并推荐合适的酒店"
    
    graph = create_react_graph()
    result = graph.invoke(create_initial_state(query, max_iterations=5))
    
    # 验证
    assert result["iteration"] > 1  # 至少1轮工具调用
    assert len(result["messages"]) > 2  # 有LLM响应和工具结果
    assert "天气" in result["answer"]  # 包含天气信息
    assert "酒店" in result["answer"]  # 包含酒店推荐
```

---

### 🚀 Phase 2：增强Agent能力（优先级：P1）

#### 2.1 实现查询改写
- [ ] 集成EnterpriseQueryRewriter
- [ ] 实现LLM查询优化
- [ ] 添加查询有效性验证

**改进rewrite_node.py：**
```python
def rewrite_node(state: TravelAgentState) -> Dict[str, Any]:
    """查询改写节点：真实版本"""
    query = state["query"]
    
    # 使用LLM改写查询
    from src.modules.module_2_advanced_rag.query_rewriter import EnterpriseQueryRewriter
    
    rewriter = EnterpriseQueryRewriter()
    rewritten = rewriter.rewrite(query)
    
    logger.info(f"🔄 查询改写：'{query}' → '{rewritten}'")
    
    return {"rewritten_query": rewritten}
```

#### 2.2 添加更多工具
- [ ] 差旅政策查询工具
- [ ] 费用计算工具
- [ ] 城市对比工具

#### 2.3 优化循环控制
- [ ] 检测重复工具调用
- [ ] 动态调整max_iterations
- [ ] 添加置信度评估

---

### 📊 Phase 3：测试和验证（优先级：P1）

#### 3.1 真实场景测试
