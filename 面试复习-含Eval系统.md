# 企业差旅管理系统 - 面试复习速查表（含 Eval 系统）

> 60秒快速回答版本 + 深入技术细节
> 更新日期：2026-06-05

---

## 🎯 项目一句话介绍

用 LangChain 构建的企业差旅智能体，实现了复杂度评估、任务分解、混合检索、三层记忆和**完整的三层评估系统**，工具调用率从0%提升到100%。

---

## 📋 核心功能模块（7大模块）

1. **基础 RAG** - FAISS向量检索
2. **高级 RAG** - BM25+Dense三路召回+RRF融合
3. **ReAct Agent** - 工具调用（天气、航班、酒店）
4. **Multi-Agent** - Supervisor协调多个Worker
5. **Chain组合** - 并行链、顺序链
6. **三层记忆系统** - 短期+工作+长期记忆
7. **⭐ Eval评估系统** - 三层评分器架构（新增）

---

## ⭐ 新增：Eval 评估系统（重点）

### 快速回答（30秒）

> "我为这个项目设计了完整的评估系统，采用**三层评分器架构**：
> 
> 1. **Code-based**（30%权重）- 确定性检查工具调用和政策合规，零成本、<10ms执行
> 2. **Model-based**（60%权重）- 集成LLM-as-Judge评估答案质量，评估正确性、相关性、忠实度
> 3. **Human**（10%权重）- 人工校准和黄金数据集构建
> 
> 设计了10个测试用例覆盖6大场景，重点强化**政策合规检查**（预算、舱位限制），政策违规直接零分阻断。基于Anthropic 2024-2026最佳实践，参考了Claw-Eval、AdaRubric等论文。"

### 深入技术细节

#### 1. 为什么需要 Eval 系统？

**三大问题**：
- 主观性强 - 不同人对"好"的标准不一致
- 规模受限 - 人工无法测试大量样本
- 难以迭代 - 缺乏量化指标，无法追踪改进

**解决方案**：自动化评估 + 标准化指标

#### 2. 三层评分器详解

**Layer 1: Code-based 确定性评分器**
```python
# 权重分配
total_score = (
    0.3 * tool_accuracy +      # 工具调用
    0.5 * policy_compliance +  # 政策合规（最重要）
    0.2 * output_format        # 输出格式
)

# 政策合规检查（CRITICAL 维度）
if budget > max_budget:
    score = 0.0  # 预算超标直接零分
    level = CRITICAL  # 阻断发布
```

**Layer 2: Model-based LLM评分器**
```python
评估维度 = {
    "Correctness": 0.35,      # 答案正确性
    "Relevance": 0.30,        # 答案相关性
    "Groundedness": 0.20,     # 基于检索，无幻觉
    "Retrieval Relevance": 0.15  # 检索质量
}
```

**Layer 3: Human 人工评分器**
- 校准 Model-based（每月20-50样本）
- 处理边缘案例
- 构建黄金数据集

#### 3. 核心指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 最终评分 | ≥ 0.85 | 加权综合分 |
| 通过率 | ≥ 85% | PASS级别占比 |
| **政策合规率** | **100%** | **零CRITICAL违规** |
| 工具调用准确率 | ≥ 90% | 正确使用工具 |

#### 4. 10个测试用例

| 场景 | 数量 | 示例 |
|------|------|------|
| 简单预订 | 3 | 机票、酒店、天气 |
| 组合查询 | 2 | 机票+酒店 |
| 复杂规划 | 2 | 多城市、紧急改签 |
| **政策合规测试** | 2 | **预期CRITICAL失败** |
| 边缘案例 | 1 | 模糊查询 |

---

## 🎤 高频面试问题

### Q1: 为什么需要 Eval 系统？

> "在开发 AI Agent 时，我们面临三大挑战：主观性强、规模受限、难以迭代。我设计了三层评估系统，从'能用'进化到'好用'，建立持续改进的闭环。"

### Q2: 三层评分器权重如何分配？

> "采用 **30% Code + 60% Model + 10% Human**。Code-based 快速筛查，Model-based 主力评估，Human 最终标准。对于企业差旅场景，政策合规权重提到50%，因为预算超标零容忍。"

### Q3: 如何保证政策合规？

> "在 Code-based 评分器中设计了 **CRITICAL 维度**：预算超标直接零分，50%权重，任何违规都会阻断发布。从技术层面强制执行政策。"

### Q4: 评估成本如何？

> "非常可控：Code-based 免费，Model-based 约1-2元/10样本。可以先用 Code-based 快速迭代，重要版本再启用 Model-based。"

### Q5: 如何持续优化？

> "采用 **Hill-Climbing 策略**：每次只改一个变量（Prompt、模型、示例），用 Eval 验证效果。定期评估：每次提交回归测试，每周完整评估，每月人工审核。"

---

## 💡 其他核心技术（原有）

### 1. 解决弱模型工具调用不可靠

```python
complexity = assess(query)
if complexity == SIMPLE:
    单工具预编排
elif complexity == MEDIUM:
    循环执行
else:
    任务分解+拓扑排序
```
**效果**：工具调用率 0% → 100%

### 2. 混合判断策略（80%规则+20%LLM）

**效果**：准确率90%，延迟<500ms，成本节省80%

### 3. 三路召回混合检索

BM25 + Dense原始 + Dense改写 → RRF融合

**效果**：单路50-60% → 三路80%

### 4. 三层记忆系统

短期（20条）+ 工作（30分钟）+ 长期（无限）

**效果**：个性化推荐，"您已第3次查询北京"

---

## 🎯 60秒电梯演讲

> "我用 LangChain 构建了企业差旅智能体，核心创新四点：
> 
> 1. **解决弱模型问题** - 工具调用率 0%→100%
> 2. **混合检索** - 三路召回，准确率80%
> 3. **三层记忆** - 个性化推荐
> 4. **⭐ 完整评估系统** - 三层评分器，政策合规零容忍
> 
> 基于 Anthropic 最佳实践，10个测试用例，建立'评估→改进→验证'闭环。对比 Spring AI，LangChain 有 LangSmith 可观测性和完整 Eval 生态。"

---

## ✅ 复习清单

**必须掌握**：
- [ ] 三层评分器架构（30% + 60% + 10%）
- [ ] Code-based 政策合规（CRITICAL）
- [ ] 10个测试用例场景
- [ ] 评估指标（≥0.85, ≥85%, 100%合规）
- [ ] 复杂度评估框架
- [ ] 三路召回混合检索
- [ ] 三层记忆系统

**加分项**：
- [ ] Hill-Climbing 优化
- [ ] Anthropic 论文（Claw-Eval）
- [ ] 成本估算（1-2元/10样本）
- [ ] 与 Spring AI 对比

---

**版本**：v2.0（新增 Eval 系统）  
**更新日期**：2026-06-05  
**建议复习时间**：30分钟
